# 📚 Grade Calculator (โปรแกรมคำนวณเกรดนักเรียน)

เว็บแอปพลิเคชันสำหรับคำนวณเกรดและสถานะการเรียน (Pass/Fail) ของนักเรียน โดยคำนวณจากคะแนนสอบย่อย (Midterm, Final, Assignment) พร้อมระบบบันทึกข้อมูลอัตโนมัติลงใน Browser

## ✨ ฟีเจอร์หลัก (Features)

- **Weighted Calculation:** คำนวณคะแนนรวมตามน้ำหนักที่กำหนด:
  - Midterm: 20%
  - Final: 50%
  - Assignment: 30%
- **Data Persistence:** บันทึกข้อมูลลง **LocalStorage** ข้อมูลไม่หายแม้กด Refresh หรือปิดหน้าเว็บ
- **Auto-load:** โหลดข้อมูลเก่ามาแสดงผลทันทีที่เปิดหน้าเว็บ
- **Input Validation:** ตรวจสอบความถูกต้องของข้อมูล (ห้ามเป็นค่าว่าง, ต้องเป็นตัวเลข, คะแนนต้องอยู่ระหว่าง 0-100)
- **Dynamic UI:** แสดงสถานะ **PASS (สีเขียว)** และ **FAIL (สีแดง)** อัตโนมัติ
- **Clean Code:** แยกการทำงานระหว่าง Logic และ UI ชัดเจน ใช้ `addEventListener` แทน `onclick`

## 🛠 เทคโนโลยีที่ใช้ (Tech Stack)

- **HTML5:** โครงสร้างหน้าเว็บ
- **CSS3:** ตกแต่งความสวยงาม (Sarabun Font, Responsive layout)
- **JavaScript (ES6):**
  - DOM Manipulation
  - Event Listeners (`DOMContentLoaded`, `click`)
  - LocalStorage API
  - JSON handling (`JSON.parse`, `JSON.stringify`)
  - Array Methods (`.map()`, `.push()`)

## 🚀 วิธีใช้งาน (How to use)

1.  เปิดไฟล์ `.html` ผ่าน Web Browser (Chrome, Edge, Safari ฯลฯ)
2.  **กรอกข้อมูล:** ชื่อ, วิชา, และคะแนนทั้ง 3 ช่อง
3.  กดปุ่ม **Calculate**:
    - ระบบจะคำนวณเกรดและแสดงผลลงในตารางทันที
    - ข้อมูลจะถูกบันทึกลงในหน่วยความจำเครื่อง
4.  กดปุ่ม **Clear** เพื่อล้างช่องกรอกข้อมูล (ข้อมูลในตารางจะยังอยู่)
5.  หากต้องการล้างข้อมูลทั้งหมดในตาราง ให้เปิด Console (F12) แล้วใช้คำสั่ง `localStorage.removeItem('studentData')` หรือสร้างปุ่ม Reset เพิ่มเติม

## 📐 เกณฑ์การให้คะแนน (Grading Criteria)

| คะแนนรวม (Score) | เกรด (Grade) |            สถานะ (Status)             |
| :--------------- | :----------: | :-----------------------------------: |
| 80 - 100         |      A       | <span style="color:green">PASS</span> |
| 70 - 79          |      B       | <span style="color:green">PASS</span> |
| 60 - 69          |      C       | <span style="color:green">PASS</span> |
| 50 - 59          |      D       | <span style="color:green">PASS</span> |
| 0 - 49           |      F       |  <span style="color:red">FAIL</span>  |

## 📂 โครงสร้างโค้ด (Code Structure)

- `handleCalculate()`: ฟังก์ชันหลัก ควบคุมการรับค่า ตรวจสอบ และสั่งบันทึก
- `validateScore(score)`: ตรวจสอบว่าคะแนนเป็นตัวเลขและอยู่ในช่วง 0-100
- `saveToStorage(data)`: บันทึก Object ข้อมูลลง LocalStorage
- `loadFromStorage()`: ดึงข้อมูลจาก LocalStorage
- `window.addEventListener("DOMContentLoaded")`: โหลดข้อมูลเก่ามาแสดงผลด้วย `.map()` เมื่อเปิดหน้าเว็บ

---

_Developed by [ใส่ชื่อของคุณ] for Education Purpose._
